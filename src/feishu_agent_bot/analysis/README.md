# Analysis

The analysis package profiles structured datasets and selects deterministic
analysis tools that can support professional reports.

Guidelines:

- Prefer structured parser output over ad hoc string extraction.
- Preserve dataset IDs, column names, units, and source locators.
- Do not infer missing values when building charts or tables.
- Keep generated analysis results traceable to datasets and evidence.
